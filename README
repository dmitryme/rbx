This is a web "replacement" of rb(report browser) based on httd + jQuery.

How to build:

edit src/log_viewer_sup.erl and uncomment CHILD(rbx_inets, worker, []) if you want to start web UI with
application start

then

./rebar compile

How to start:
application:start(rbx).

and go http://localhost:8000 in you web browser

Possible config parameters:
- inets_port - http port can be configured via config file or "erl -rbx inets_port 8001"
- report_dir - path to logs. If no parameter is found, looking for sasl error_logger_mf_dir param.
               Can be configured the same way as inets_port. E.g. "erl -rbx report_dir "/tmp/log""